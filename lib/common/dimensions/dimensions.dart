import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppDimensions {
  static const desktopScreenSize = Size(1440, 1024);
  static final desktopScreenHeight = desktopScreenSize.height.h;
  static final desktopScreenWidth = desktopScreenSize.width.w;
  static const phoneScreenSize = Size(375, 813);
  static final phoneScreenHeight = phoneScreenSize.height.h;
  static final phoneScreenWidth = phoneScreenSize.width.w;
}
